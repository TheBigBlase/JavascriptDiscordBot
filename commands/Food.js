const Discord = require('discord.js');
const settings = require('../settings.json');
const request = require('request');

module.exports = {
    food: async (message, client) => {
        request();
    }
}